<!-- dorfl-sidecar: item=task:document-v0-subset-limits type=task slug=document-v0-subset-limits allAnswered=false -->

## Q1

**'task:document-v0-subset-limits' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The doc omits the ratified v0 limit that block-box background-color and border colours are NOT cascaded end-to-end: paintBox leaves block/inline_box/anonymous boxes transparent and colours are caller-supplied above the paint seam via PaintStyle/GoldenScene. The doc lists background-color as a fully supported property (sec 2) and describes the box model with border edges (sec 3) with no caveat, so a reader concludes a div style=background-color:red paints red in v0 (it does not). Add the limit. (work/notes/observations/box-colours-not-cascaded-end-to-end-2026-07-14.md ratified ACCEPT and states the document-v0-subset-limits doc MUST state this explicitly; src/paint.zig:298-304 leaves boxes transparent; docs/v0-subset.md:66,132 has no such note. Violates acceptance criterion 5 (matches reality, not aspirational superset).)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q1 fields: id=q1 kind=stuck -->

**Your answer** (write below this line):

## Q2

**'task:document-v0-subset-limits' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The doc omits the ratified v0 limit that block-box background-color and border colours are NOT cascaded end-to-end: paintBox leaves block/inline_box/anonymous boxes transparent and colours are caller-supplied above the paint seam via PaintStyle/GoldenScene. The doc lists background-color as a fully supported property (sec 2) and describes the box model with border edges (sec 3) with no caveat, so a reader concludes a div style=background-color:red paints red in v0 (it does not). Add the limit. (work/notes/observations/box-colours-not-cascaded-end-to-end-2026-07-14.md ratified ACCEPT and states the document-v0-subset-limits doc MUST state this explicitly; src/paint.zig:298-304 leaves boxes transparent; docs/v0-subset.md:66,132 has no such note. Violates acceptance criterion 5 (matches reality, not aspirational superset).)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q2 fields: id=q2 kind=stuck -->

**Your answer** (write below this line):

## Q3

**'task:document-v0-subset-limits' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The doc omits the ratified v0 limit that block-box background-color and border colours are NOT cascaded end-to-end: paintBox leaves block/inline_box/anonymous boxes transparent and colours are caller-supplied above the paint seam via PaintStyle/GoldenScene. The doc lists background-color as a fully supported property (sec 2) and describes the box model with border edges (sec 3) with no caveat, so a reader concludes a div style=background-color:red paints red in v0 (it does not). Add the limit. (work/notes/observations/box-colours-not-cascaded-end-to-end-2026-07-14.md ratified ACCEPT and states the document-v0-subset-limits doc MUST state this explicitly; src/paint.zig:298-304 leaves boxes transparent; docs/v0-subset.md:66,132 has no such note. Violates acceptance criterion 5 (matches reality, not aspirational superset).)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q3 fields: id=q3 kind=stuck -->

**Your answer** (write below this line):

## Q4

**'task:document-v0-subset-limits' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The doc omits the ratified v0 limit that block-box background-color and border colours are NOT cascaded end-to-end: paintBox leaves block/inline_box/anonymous boxes transparent and colours are caller-supplied above the paint seam via PaintStyle/GoldenScene. The doc lists background-color as a fully supported property (sec 2) and describes the box model with border edges (sec 3) with no caveat, so a reader concludes a div style=background-color:red paints red in v0 (it does not). Add the limit. (work/notes/observations/box-colours-not-cascaded-end-to-end-2026-07-14.md ratified ACCEPT and states the document-v0-subset-limits doc MUST state this explicitly; src/paint.zig:298-304 leaves boxes transparent; docs/v0-subset.md:66,132 has no such note. Violates acceptance criterion 5 (matches reality, not aspirational superset).)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q4 fields: id=q4 kind=stuck -->

**Your answer** (write below this line):

## Q5

**'task:document-v0-subset-limits' was bounced — how should we proceed?**

> PR/code review (Gate 2) blocked this work:
> - The doc omits the ratified v0 limit that block-box background-color and border colours are NOT cascaded end-to-end: paintBox leaves block/inline_box/anonymous boxes transparent and colours are caller-supplied above the paint seam via PaintStyle/GoldenScene. The doc lists background-color as a fully supported property (sec 2) and describes the box model with border edges (sec 3) with no caveat, so a reader concludes a div style=background-color:red paints red in v0 (it does not). Add the limit. (work/notes/observations/box-colours-not-cascaded-end-to-end-2026-07-14.md ratified ACCEPT and states the document-v0-subset-limits doc MUST state this explicitly; src/paint.zig:298-304 leaves boxes transparent; docs/v0-subset.md:66,132 has no such note. Violates acceptance criterion 5 (matches reality, not aspirational superset).)
> PR/code review (Gate 2) did not reach a unanimous approve across reviewMaxRounds=2 round(s) (a block is terminal and is never re-rolled); forcing needs-attention (never silently merged or looped).

<!-- q5 fields: id=q5 kind=stuck -->

**Your answer** (write below this line):
